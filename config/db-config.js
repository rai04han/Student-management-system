// Database Configuration - Student Management System

const DB_CONFIG = {
    host: "localhost",
    port: 3306,
    database: "student_db",
    charset: "utf8"
};

const DB_TABLES = {
    students: "tbl_students",
    courses: "tbl_courses",
    grades: "tbl_grades",
    users: "tbl_users"
};